import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neebooks.niwj',
  appName: '倪柝声文集',
  webDir: 'dist',
  android: {
    allowMixedContent: true,
  },
};

export default config;
